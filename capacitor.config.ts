import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.mak.proyectoangular',
  appName: 'proyecto-angular',
  webDir: 'www'
};

export default config;
